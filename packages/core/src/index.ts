/**
 * src/index.ts
 *
 * Public export of the Core:
 * - optimize()
 * - core types (Policy, Report, AuditEvent)
 *
 * Final rule to keep in mind:
 * If it has HTTP, it is not Core.
 * The Core is a pure, deterministic transformation pipeline.
 */
