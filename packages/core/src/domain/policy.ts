/**
 * domain/policy.ts
 *
 * Defines the Policy object and defaults:
 * - thresholds (e.g. maxReductionPct)
 * - stage toggles (normalize/prune/compress)
 * - protection lists (untouchable roles, forbidden patterns)
 */
