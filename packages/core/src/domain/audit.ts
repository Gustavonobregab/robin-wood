/**
 * domain/audit.ts
 *
 * Defines AuditEvent (explainability):
 * - event type (REMOVE / TRUNCATE / DEDUPE / KEEP)
 * - target (messageId, range)
 * - reason (enum)
 * - details (before/after sizes, etc.)
 */
